// P1 Duplicated Widgets Batch 1 (May 30, 2026)
// Stub / remnant from prior tabbed sauce selector (logic inlined in customization_modal.dart).
// No src/ imports. No debug prints. FranchiseProvider not needed (presentational stub).
// Mobile canonical; safe to delete in next batch if fully dead.

import 'package:flutter/material.dart';

class PizzaSauceSelectorTab extends StatelessWidget {
  const PizzaSauceSelectorTab({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
