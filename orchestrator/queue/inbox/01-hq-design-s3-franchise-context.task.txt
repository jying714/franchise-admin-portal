# id: hq-design-s3-franchise-context
# agent: web_frontend
# priority: 10

Using web-app/lib/admin/hq_owner/screens/design_branding_screen.dart:

Quote the exact first 10–12 lines of the real file.
Then quote the franchise context Text region (the line that shows franchiseId or "not selected").

Propose ONLY a small polish to that franchise context label if useful (clearer wording, still using existing FranchiseProvider.franchiseId / hasFranchise only). Do not invent fields, do not add new providers, do not change the Card or AppBar.

If the current label already satisfies S3 (franchise id when selected, clear not-selected state) → reply only: No change needed.

Show exact ## BEFORE / ## AFTER for that region only.
