# id: xai-foundation-mark-key-v2
# agent: web_frontend
# backend: xai
# priority: 7

backend: xai
Role: web_frontend

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/onboarding_menu_foundation_screen.dart

Task:
Quote the exact first 10–12 lines of the loaded file.

Goal: On Save Foundation & Continue (when existing progress threshold allows), ensure product key is marked:
```dart
await Provider.of<shared.OnboardingProgressProvider>(context, listen: false)
    .markStepComplete('onboarding_menu_foundation');
```
or the equivalent already used in-file if it already uses shared.OnboardingProgressProvider.

Hard rules:
- Key string must be exactly `onboarding_menu_foundation` (not a tab sub-key).
- Use **shared.OnboardingProgressProvider** only — no new import path for progress.
- Do **not** navigate via AdminDashboardScreen.switchToSection.
- Prefer HQ shell / existing in-file navigation only; if navigation is already HQ-safe, leave nav alone and only fix the mark if needed.
- Prefer **one** BEFORE/AFTER region on the Save/Continue handler only.

If the continue handler already marks `onboarding_menu_foundation` with shared.OnboardingProgressProvider and does not use Admin switchToSection → reply only: No change needed

Rules:
1. Quote-first.
2. Edit only this file.
3. Max 2 regions.
4. Proposal only.
