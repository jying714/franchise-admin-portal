# id: t7-02-settings-help-escape
# agent: web_frontend
# priority: 61

Using web-app/lib/widgets/header/settings_icon_button.dart and web-app/lib/widgets/header/help_icon_button.dart:

1. Quote the exact first 10–12 lines of BOTH real files.
2. You may change ONLY ONE region in settings_icon_button.dart. Do NOT edit help_icon_button.dart at all.
3. Check whether the Settings Icon color already uses DesignTokens.primaryColor.
4. If it already uses DesignTokens.primaryColor → reply ONLY with: No change needed.
5. If it does not, set color to DesignTokens.primaryColor (never currentPrimaryColor, never *ColorHex as Color). BEFORE must be contiguous from injected source.
6. Show exact ## BEFORE / ## AFTER for that one region only, or No change needed. Do not invent members. Do not touch the sibling file.
