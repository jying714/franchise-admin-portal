# id: 70-materialapp-theme-franchise-rebuild
# agent: web_frontend
# backend: xai
# priority: 95

backend: xai
max_regions: 2
Role: web_frontend

Files:
- web-app/lib/main.dart

Task: Quote first 10–12 lines of main.dart, then the FranchiseAuthenticatedRoot build method where MaterialApp is constructed (theme: ThemeData(... primaryColor: DesignTokens.primaryColor ...)).

Product outcome: Authenticated MaterialApp ThemeData must rebuild when FranchiseProvider branding changes so HQ dashboard chrome (colorScheme.primary, etc.) follows the selected franchise without leaving the route.

Do this by watching FranchiseProvider (or currentConfigVersion) in the same build that constructs MaterialApp — e.g. context.watch / Consumer / Selector on currentConfigVersion — so ThemeData is recreated after setBrandingFromFranchiseDoc / setFranchiseId.

Rules:
1. Quote-first.
2. Edit only main.dart.
3. Do not invent DesignTokens fields or FranchiseProvider() zero-arg.
4. Keep DesignTokens.setFranchiseProvider bootstrap as-is.
5. Prefer Selector/Consumer over rewriting the whole shell.
6. If MaterialApp already rebuilds on franchise branding notify → No change needed.
