# id: 85-design-tokens-verify-primary-bridge
# agent: web_frontend
# backend: xai
# priority: 50

backend: xai
max_regions: 1
Role: web_frontend

Files:
- web-app/lib/config/design_tokens.dart

Task: Quote setFranchiseProvider, primaryColor, secondaryColor, currentAppName, currentLogoUrl getters.

Product outcome: Confirm primary/secondary read _fp?.currentPrimaryColorHex / currentSecondaryColorHex with shared.DesignTokens hex fallback; appName/logo from _fp. VERIFY ONLY — if bridge is correct → No change needed. Do not add new fields/getters.

Rules:
1. Quote-first.
2. HARD BAN if proposing new DesignTokens members.
3. Default expectation: No change needed.
