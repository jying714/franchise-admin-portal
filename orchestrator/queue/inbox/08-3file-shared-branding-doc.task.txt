# id: 08-3file-shared-branding-doc
# agent: mobile_shared
# priority: 80

Using these THREE files:
- packages/shared_core/lib/src/core/config/branding_config.dart
- packages/shared_core/lib/src/core/config/design_tokens.dart
- packages/shared_core/lib/src/core/config/ui_config.dart

1. Quote the exact first 10–12 lines of ALL THREE real files.
2. Propose ONLY a short class-level or section docstring/comment on branding_config.dart that clarifies it is the static defaults model and that runtime franchise-scoped branding is owned by FranchiseProvider (live path via DesignTokens / UiConfig). No new fields, no getters, no logic or serialization changes.
3. Do not edit design_tokens.dart or ui_config.dart beyond quoting them.
4. Show exact ## BEFORE / ## AFTER for that one comment region only.
5. If any required file cannot be quoted → reply only: FAILED TO LOAD
