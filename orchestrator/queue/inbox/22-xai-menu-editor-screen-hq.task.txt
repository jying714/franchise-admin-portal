# id: xai-menu-editor-screen-hq
# agent: web_frontend
# backend: xai
# priority: 6

backend: xai
Role: web_frontend

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/menu_item_editor_screen.dart

Task:
Quote the exact first 10–12 lines of the loaded file.

Inspect for any AdminDashboardScreen navigation or `/admin/dashboard` routes.
If found, replace with HQ shell pattern:
```dart
final hqShell =
    context.findAncestorStateOfType<HqOnboardingShellScreenState>();
if (hqShell != null) {
  hqShell.switchToSection(sectionKey);
  return;
}
```
Add HQ shell import only if needed.
If the file has no Admin navigation and is already HQ-safe → reply only: No change needed

Do not expand scope beyond navigation hygiene.

Rules:
1. Quote-first.
2. Edit only this file.
3. Proposal only.
