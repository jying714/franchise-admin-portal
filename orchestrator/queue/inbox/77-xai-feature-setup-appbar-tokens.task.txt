# id: xai-feature-setup-appbar-tokens-v2
# agent: web_frontend
# backend: xai
# priority: 6
# max_regions: 1
# context: coding_min

backend: xai
Role: web_frontend
max_regions: 1

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/onboarding_feature_setup_screen.dart

Goal: Main Scaffold AppBar uses DesignTokens.appBar* if those getters exist on DesignTokens.

Task:
Quote first 10–12 lines.
Respond ONLY with FILE / BEFORE / AFTER.

Locate non-loading AppBar backgroundColor / elevation / iconTheme.

AFTER only if DesignTokens.appBarBackgroundColor, appBarElevation, appBarIconColor exist in design_tokens (already imported):
use those three. If getters do not exist → No change needed (do not invent DesignTokens fields).

Do not change Save path.
