# id: t2-10-maintenance-error-color
# agent: web_frontend
# priority: 19

Using web-app/lib/widgets/dashboard/maintenance_banner.dart and web-app/lib/config/design_tokens.dart:

1. Quote the exact first 10–12 lines of BOTH real files.
2. Propose ONLY one small change so that the MaterialBanner backgroundColor uses DesignTokens.errorColor (existing Color getter — NEVER *ColorHex strings treated as Color).
3. BEFORE must be a contiguous region copied EXACTLY from the injected source. If you cannot find an exact match → reply only: FAILED TO LOAD.
4. If backgroundColor already uses DesignTokens.errorColor → reply ONLY with: No change needed.
5. Do not add fields, getters, methods, or change any other logic. Keep the change surgical.
6. Show exact ## BEFORE / ## AFTER for that tiny region only (fences must differ, or No change needed).
