# id: xai-branding-class-doc-v11
# agent: web_frontend
# backend: xai
# priority: 4
# max_regions: 1
# context: coding_min

backend: xai
Role: web_frontend
max_regions: 1

Files:
- web-app/lib/admin/hq_owner/screens/design_branding_screen.dart

Goal: Class dartdoc must not claim "No Firestore writes in v1" — Save is v1.1 live.

Task:
Quote the exact first 10–12 lines of the loaded file.
Respond ONLY with FILE / BEFORE / AFTER.

Locate class-level dartdoc lines that say v1 shell / No Firestore writes:
```dart
/// HQ Owner — Design & Branding screen (v1 shell).
///
/// Slice: docs/slices/hq-design-branding-v1.md (Decision 8).
/// Opened from Owner HQ Live Branding card via Navigator.push + MaterialPageRoute.
/// Back pops to the dashboard. No Firestore writes in v1.
///
/// S1: scaffold + AppBar + placeholder only.
/// Later steps fill preview, draft fields, and Save snackbar.
```

AFTER (same region, truth-aligned):
```dart
/// HQ Owner — Design & Branding screen (v1.1).
///
/// Slice: docs/slices/hq-design-branding-v1.md (Decision 8).
/// Opened from Owner HQ Live Branding card via Navigator.push + MaterialPageRoute.
/// Back pops to the dashboard. Save merges branding to franchise + config/ui_config.
///
/// Draft fields drive live preview; Save persists and refreshes DesignTokens path.
```

Docstring-only. No logic changes.
If dartdoc already says v1.1 / Save persists → No change needed
