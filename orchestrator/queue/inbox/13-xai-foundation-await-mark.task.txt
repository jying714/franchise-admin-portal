# id: xai-foundation-await-mark
# agent: web_frontend
# backend: xai
# priority: 6

backend: xai
Role: web_frontend

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/onboarding_menu_foundation_screen.dart

Task:
Quote the exact first 10–12 lines of the loaded file.

Reliability polish: Save Foundation & Continue already calls the correct product key, but the call is fire-and-forget (no await). Make the mark durable before navigation.

Locate the ElevatedButton onPressed success path:
```dart
if (foundationProgress >= 0.8) {
  progressProvider
      .markStepComplete('onboarding_menu_foundation');

  final hqShell = context.findAncestorStateOfType<
      HqOnboardingShellScreenState>();
  if (hqShell != null) {
    hqShell.switchToSection('onboardingMenuItems');
    return;
  }
  ...
}
```

Required change:
1. Make the onPressed callback `async`.
2. `await progressProvider.markStepComplete('onboarding_menu_foundation');` before the HQ shell switch.
3. Keep key exactly `onboarding_menu_foundation`.
4. Keep HQ shell navigation; do not introduce AdminDashboardScreen.
5. Do not change tab screens, progress threshold, or sub-key logic.

APPLY SAFETY:
- Prefer one BEFORE/AFTER region around the button onPressed.
- Do not remove imports.

If markStepComplete is already awaited before switchToSection → reply only: No change needed

Rules:
1. Quote-first.
2. Edit only this file.
3. Proposal only.
