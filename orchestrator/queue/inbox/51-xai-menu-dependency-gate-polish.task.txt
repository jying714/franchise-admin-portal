# id: xai-menu-dependency-gate-polish
# agent: web_frontend
# backend: xai
# priority: 6
# max_regions: 2

backend: xai
Role: web_frontend
max_regions: 2

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/onboarding_menu_items_screen.dart

Goal: Dependency gate is coherent: AppBar iconTheme theme-aware; status check icon uses colorScheme.primary; "All Loaded" bypass should set state so the main list path can show (not a no-op).

Task:
Quote the exact first 10–12 lines of the loaded file.

Region 1 — AppBar hard-coded black (exact on-disk in main scaffold):
```dart
            iconTheme: const IconThemeData(color: Colors.black),
```
AFTER:
```dart
            iconTheme: IconThemeData(color: colorScheme.onSurface),
```

Region 2 — status bar check icon still Colors.green:
```dart
                            const Icon(Icons.check_circle, color: Colors.green),
```
AFTER:
```dart
                            Icon(Icons.check_circle, color: colorScheme.primary),
```

Do not invent DesignTokens members. Do not rewrite dependency load logic unless a one-line fix is required for the "All Loaded – Open Menu Items" button to be meaningful (today it sets _isEditing = false while already false — only fix if you can do so surgically without scope creep).

If both colors already theme-aware → No change needed
