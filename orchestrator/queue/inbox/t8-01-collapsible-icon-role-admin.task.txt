# id: t8-01-collapsible-icon-role-admin
# agent: web_frontend
# priority: 70

Using web-app/lib/widgets/collapsible_panel.dart and web-app/lib/widgets/dashboard/role_badge.dart:

DUAL-EDIT PILOT — change exactly ONE region in EACH file (two independent micro-edits).

1. Quote the exact first 10–12 lines of BOTH real files.
2. In collapsible_panel.dart ONLY: set expand/collapse Icon color to DesignTokens.primaryColor (never currentPrimaryColor, never *ColorHex as Color). If already DesignTokens.primaryColor → that file needs no change.
3. In role_badge.dart ONLY: for case 'Admin', set badgeColor to DesignTokens.secondaryColor (never currentSecondaryColor, never *ColorHex as Color). If already DesignTokens.secondaryColor → that file needs no change.
4. Do not rewrite whole widgets. Do not change any other regions (collapsible title, Owner/default cases, etc.).
5. Show labeled pairs:
   ## BEFORE collapsible_panel.dart / ## AFTER collapsible_panel.dart
   ## BEFORE role_badge.dart / ## AFTER role_badge.dart
   Or for a file that is already correct, write: No change needed for <path>.
6. BEFORE regions must be contiguous copies from injected source. If either file cannot be quoted → reply only: FAILED TO LOAD.
