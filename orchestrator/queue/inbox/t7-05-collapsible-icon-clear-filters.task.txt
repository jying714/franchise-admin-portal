# id: t7-05-collapsible-icon-clear-filters
# agent: web_frontend
# priority: 64

Using web-app/lib/widgets/collapsible_panel.dart and web-app/lib/widgets/clear_filters_button.dart:

1. Quote the exact first 10–12 lines of BOTH real files.
2. You may change ONLY ONE region in collapsible_panel.dart. Do NOT edit clear_filters_button.dart at all.
3. Propose ONLY one small change so the expand/collapse Icon color uses DesignTokens.primaryColor (never currentPrimaryColor, never *ColorHex as Color).
4. BEFORE must be a contiguous region copied EXACTLY from the injected source (Icon( … color: colorScheme.primary )). If you cannot find an exact match → reply only: FAILED TO LOAD.
5. If that Icon already uses DesignTokens.primaryColor → reply ONLY with: No change needed.
6. Show exact ## BEFORE / ## AFTER for that one region only (fences must differ), or No change needed. Do not change the title Text in the same proposal. Do not touch the sibling file.
