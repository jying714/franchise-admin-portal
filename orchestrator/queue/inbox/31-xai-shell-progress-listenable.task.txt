# id: xai-shell-progress-listenable
# agent: web_frontend
# backend: xai
# priority: 5

backend: xai
Role: web_frontend

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/hq_onboarding_shell_screen.dart

Task:
Quote the exact first 10–12 lines of the loaded file.

Product rule: child screens `context.watch<shared.OnboardingProgressProvider>()` must receive notifications when markStepComplete runs.

Locate:
```dart
Provider<shared.OnboardingProgressProvider>.value(
  value: Provider.of<OnboardingProgressProviderImpl>(
    context,
    listen: false,
  ),
),
```

Replace with ChangeNotifierProvider so the Impl remains listenable:
```dart
ChangeNotifierProvider<shared.OnboardingProgressProvider>.value(
  value: Provider.of<OnboardingProgressProviderImpl>(
    context,
    listen: false,
  ),
),
```

Do not change other providers in the MultiProvider list. Prefer one region.
If already ChangeNotifierProvider.value → reply only: No change needed

Rules:
1. Quote-first.
2. Edit only this file.
3. Proposal only.
