# id: 09-shared-app-config-docstring
# agent: mobile_shared
# priority: 90

Using packages/shared_core/lib/src/core/config/app_config.dart:

1. Quote the exact first 10–12 lines of the real file.
2. Propose ONLY a short class-level docstring (or improve an existing one) that states AppConfig holds static defaults and that franchise-scoped values surface through FranchiseProvider.
3. Do not add fields, getters, methods, or change any logic / serialization / mapping.
4. Show exact ## BEFORE / ## AFTER for that docstring region only.
5. If the file cannot be quoted → reply only: FAILED TO LOAD
