# id: xai-feature-save-to-foundation
# agent: web_frontend
# backend: xai
# priority: 5
# max_regions: 2

backend: xai
Role: web_frontend
max_regions: 2

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/onboarding_feature_setup_screen.dart

Goal: After successful feature Save + markStepComplete('onboarding_feature_setup'), navigate in-shell to foundation instead of maybePop.

Task:
Quote the exact first 10–12 lines of the loaded file.
Respond ONLY with FILE / BEFORE / AFTER.

Locate success path in `_handleSave` (exact on-disk pattern):
```dart
      if (success && mounted) {
        await onboarding.markStepComplete('onboarding_feature_setup');
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(content: Text(loc.saveSuccess)),
        );
        Navigator.of(context).maybePop();
      }
```

AFTER: keep mark + snackbar; replace `maybePop` with:
```dart
        final hqShell =
            context.findAncestorStateOfType<HqOnboardingShellScreenState>();
        if (hqShell != null) {
          hqShell.switchToSection('onboarding_menu_foundation');
        } else {
          Navigator.of(context).maybePop();
        }
```

If import for shell is missing, add:
`import 'package:franchise_admin_portal/admin/hq_owner/onboarding/screens/hq_onboarding_shell_screen.dart';`

Do not change `_markComplete` toggle behavior.
If success path already switches to foundation → No change needed
