# id: hq-design-s6-save-snackbar
# agent: web_frontend
# priority: 40

Using web-app/lib/admin/hq_owner/screens/design_branding_screen.dart:

Quote the exact first 10–12 lines of the real file.

Add a bottom action row (after the live preview / draft fields) with:
- Save button that is enabled and on press shows a SnackBar with the exact text "Save not wired yet" and does nothing else (no Firestore, no setBrandingFromFranchiseDoc, no persistence)
- Cancel / Reset that discards draft values back to the current live DesignTokens / FranchiseProvider values

Use only existing DesignTokens colors for button styling. No new fields, no section_registry, no Firestore writes.

Show exact ## BEFORE / ## AFTER for the actions region only.
