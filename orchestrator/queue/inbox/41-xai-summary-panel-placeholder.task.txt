# id: xai-summary-panel-placeholder
# agent: web_frontend
# backend: xai
# priority: 7

backend: xai
Role: web_frontend

Files:
- web-app/lib/admin/hq_owner/onboarding/widgets/onboarding_summary_panel.dart

Task:
Quote whatever content exists (file may be nearly empty / BOM-only).

This file is an empty stub. Provide a full-file AFTER with a minimal placeholder widget so imports do not break:
```dart
import 'package:flutter/material.dart';

/// Placeholder HQ onboarding summary panel (v1 uses ReviewSummaryTable instead).
class OnboardingSummaryPanel extends StatelessWidget {
  const OnboardingSummaryPanel({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
```

BEFORE must be the exact current file contents (even if a single blank line or BOM). If the apply path cannot match empty BEFORE, still emit FILE + AFTER as a full replacement proposal.
If a real non-empty panel already exists → reply only: No change needed

Rules:
1. Quote-first.
2. Edit only this file.
3. Proposal only.
