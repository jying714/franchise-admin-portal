# id: xai-verify-menu-toggle
# agent: web_frontend
# backend: xai
# priority: 4

backend: xai
Role: web_frontend

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/onboarding_menu_items_screen.dart

Task:
Quote the exact first 10–12 lines of the loaded file.

VERIFY-ONLY (do not invent work):
Confirm `_markComplete` toggles with:
- `isStepComplete('onboardingMenuItems')`
- `markStepIncomplete('onboardingMenuItems')` when complete
- `markStepComplete('onboardingMenuItems')` when incomplete

If present → reply only: No change needed
If missing the incomplete branch → add the toggle using shared.OnboardingProgressProvider only (same pattern as feature setup).

Rules:
1. Quote-first.
2. Edit only this file.
3. Proposal only.
