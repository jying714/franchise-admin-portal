# id: xai-ingredients-screen-back-in-shell
# agent: web_frontend
# backend: xai
# priority: 5
# max_regions: 1
# context: coding_min

backend: xai
Role: web_frontend
max_regions: 1

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/onboarding_ingredients_screen.dart

Goal: Ingredients tab AppBar leading back must not Navigator.pop out of HQ shell (tab is embedded in foundation).

Task:
Quote first 10–12 lines.
Respond ONLY with FILE / BEFORE / AFTER.

Locate AppBar leading IconButton with Navigator.of(context).pop.

AFTER: set leading: SizedBox.shrink() or automaticallyImplyLeading: false so back does not pop the shell.

Do not change save/load/mark logic.
If already no pop leading → No change needed
