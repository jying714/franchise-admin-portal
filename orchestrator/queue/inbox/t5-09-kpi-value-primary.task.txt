# id: t5-09-kpi-value-primary
# agent: web_frontend
# priority: 48

Using web-app/lib/widgets/financials/kpi_card.dart and web-app/lib/config/design_tokens.dart:

1. Quote the exact first 10–12 lines of BOTH real files.
2. Propose ONLY one small change so that the value Text color uses DesignTokens.primaryColor (the Color getter — NEVER currentPrimaryColor, NEVER *ColorHex as Color).
3. BEFORE must be a contiguous region copied EXACTLY from the injected source (the displaySmall copyWith color: colorScheme.primary region). If you cannot find an exact match → reply only: FAILED TO LOAD.
4. If that value Text already uses DesignTokens.primaryColor → reply ONLY with: No change needed.
5. Do not invent members. Do not rewrite the card.
6. Show exact ## BEFORE / ## AFTER for that tiny region only (fences must differ, or No change needed).
