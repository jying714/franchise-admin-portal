Role: web_frontend

File: web-app/lib/admin/hq_owner/onboarding/screens/onboarding_menu_items_screen.dart

Task: Quote first 10–12 lines.

Problem: Mark complete works with key onboardingMenuItems. There is no markStepIncomplete path — user cannot unmark.

Change (ONE surgical behavior):
- Keep mark complete using key exactly: onboardingMenuItems
- Wire unmark: when step is already complete (isStepComplete('onboardingMenuItems')), the same control should call markStepIncomplete('onboardingMenuItems') instead of markStepComplete.
- Do not invent new UI chrome beyond toggling the existing mark-complete entry point(s).
- Do not change other navigation or schema logic.

If toggle already exists with the correct key → reply only: No change needed.

Rules:
1. Quote-first.
2. Edit only this file.
3. No new fields/methods on providers.
4. Key must be onboardingMenuItems (never menu_items).
