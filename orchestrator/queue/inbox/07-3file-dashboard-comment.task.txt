# id: 07-3file-dashboard-comment
# agent: web_frontend
# priority: 70

Using these THREE files:
- web-app/lib/admin/hq_owner/owner_hq_dashboard_screen.dart
- web-app/lib/config/design_tokens.dart
- web-app/lib/admin/hq_owner/widgets/alerts_card.dart

1. Quote the exact first 10–12 lines of ALL THREE real files.
2. Propose ONLY a single clarifying comment (or short docstring) inside owner_hq_dashboard_screen.dart that documents the live branding path for the existing live color-swatch / onboarding cards (FranchiseProvider → DesignTokens.setFranchiseProvider → primaryColor/secondaryColor). No code logic change, no new fields, no UI additions.
3. Do not edit the other two files beyond quoting them.
4. Show exact ## BEFORE / ## AFTER for the one small comment region only.
5. If any required file cannot be quoted → reply only: FAILED TO LOAD
