# id: t3-04-collapsible-radius-no-change
# agent: web_frontend
# priority: 23

Using web-app/lib/widgets/collapsible_panel.dart and web-app/lib/config/design_tokens.dart:

1. Quote the exact first 10–12 lines of BOTH real files.
2. Propose ONLY one small change so that Card shape BorderRadius uses DesignTokens.adminCardRadius (already the live token).
3. BEFORE must be a contiguous region copied EXACTLY from the injected source. If you cannot find an exact match → reply only: FAILED TO LOAD.
4. If BorderRadius.circular already uses DesignTokens.adminCardRadius → reply ONLY with: No change needed.
5. Do not invent DesignTokens members. Do not rewrite the panel.
6. Show exact ## BEFORE / ## AFTER for that tiny region only (fences must differ, or No change needed).
