# id: t4-02-info-tile-text-already
# agent: web_frontend
# priority: 31

Using web-app/lib/widgets/info_tile.dart and web-app/lib/config/design_tokens.dart:

1. Quote the exact first 10–12 lines of BOTH real files.
2. Check whether the title Text color already uses DesignTokens.textColor.
3. If it already uses DesignTokens.textColor → reply ONLY with: No change needed.
4. Do not propose any other Color, do not invent getters, do not emit BEFORE/AFTER if already correct.
5. If it does not use DesignTokens.textColor, show exact ## BEFORE / ## AFTER for that style region only.
6. If either file cannot be quoted → reply only: FAILED TO LOAD.
