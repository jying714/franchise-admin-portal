# id: xai-hq-dashboard-progress-card-watch
# agent: web_frontend
# backend: xai
# priority: 7

backend: xai
Role: web_frontend

Files:
- web-app/lib/admin/hq_owner/owner_hq_dashboard_screen.dart

Task:
Quote the exact first 10–12 lines of the loaded file.

Product rule: HQ onboarding progress card must watch **OnboardingProgressProviderImpl** (concrete listenable), not only an abstract ProxyProvider alias.

Search the file for the onboarding progress card / section.
1. If it already `watch`/`Consumer`s `OnboardingProgressProviderImpl` → reply only: No change needed
2. If it watches only the abstract `shared.OnboardingProgressProvider` and the card does not rebuild on markStepComplete → switch the watch/Consumer type to the concrete Impl **only if** Impl is already imported/available in this file or via shared_core export used here.
3. Do not invent a new provider. Do not add fields. Prefer one region.

If no progress card exists in this file → reply only: No change needed

Rules:
1. Quote-first.
2. Edit only this file.
3. Proposal only.
4. SCOPE_CARD: no zero-arg FranchiseProvider; no new DesignTokens fields.
