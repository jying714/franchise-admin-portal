# id: xai-menu-persist-dirty-stub
# agent: web_frontend
# backend: xai
# priority: 7

backend: xai
Role: web_frontend

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/onboarding_menu_items_screen.dart

Task:
Quote the exact first 10–12 lines of the loaded file.

Locate the dirty-row Save button that still has a stub:
```dart
ElevatedButton(
  onPressed: () async {
    /* your exact persistChanges code */
  },
  child: Text(loc.saveChanges)),
```

Wire it to the existing MenuItemProvider API already used in this file:
```dart
onPressed: () async {
  final menuProvider =
      Provider.of<shared.MenuItemProvider>(context, listen: false);
  try {
    await menuProvider.persistChanges();
    if (mounted) {
      ScaffoldMessenger.of(context).showSnackBar(
        const SnackBar(content: Text('Changes saved')),
      );
    }
  } catch (e) {
    if (mounted) {
      ScaffoldMessenger.of(context).showSnackBar(
        SnackBar(content: Text('Save failed: $e')),
      );
    }
  }
},
```

Do not invent new service methods. Prefer one region.
If Save already calls `persistChanges()` for real → reply only: No change needed

Rules:
1. Quote-first.
2. Edit only this file.
3. Proposal only.
