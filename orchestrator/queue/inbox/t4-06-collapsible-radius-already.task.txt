# id: t4-06-collapsible-radius-already
# agent: web_frontend
# priority: 35

Using web-app/lib/widgets/collapsible_panel.dart and web-app/lib/config/design_tokens.dart:

1. Quote the exact first 10–12 lines of BOTH real files.
2. Check whether Card shape BorderRadius.circular already uses DesignTokens.adminCardRadius.
3. If it already uses DesignTokens.adminCardRadius → reply ONLY with: No change needed.
4. Do not invent radius tokens. Do not rewrite the panel. Do not emit BEFORE/AFTER if already correct.
5. Only if adminCardRadius is missing, show exact ## BEFORE / ## AFTER for that shape region only.
6. If either file cannot be quoted → reply only: FAILED TO LOAD.
