# id: xai-foundation-handoff-menu
# agent: web_frontend
# backend: xai
# priority: 5
# max_regions: 2

backend: xai
Role: web_frontend
max_regions: 2

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/onboarding_menu_foundation_screen.dart

Goal: After successful foundation Save & Continue (markStepComplete onboarding_menu_foundation), navigate in-shell to Menu Items.

Task:
Quote the exact first 10–12 lines of the loaded file.

Locate the Save & Continue / foundation complete path that already awaits:
markStepComplete('onboarding_menu_foundation')

AFTER that successful mark (and any existing snackbar), add in-shell navigation:
```dart
final hqShell =
    context.findAncestorStateOfType<HqOnboardingShellScreenState>();
hqShell?.switchToSection('onboardingMenuItems');
```

Import HqOnboardingShellScreen / State only if not already imported (path already used elsewhere under hq_owner/onboarding).

Do not mark other product keys. Do not invent Admin dashboard routes.
If handoff already switches to onboardingMenuItems after success → No change needed

Constraints:
- Edit only this file
- shared.OnboardingProgressProvider only for marks
