# id: xai-franchise-switch-stay-surface
# agent: web_frontend
# backend: xai
# priority: 2
# max_regions: 1
# context: coding_min

backend: xai
Role: web_frontend
max_regions: 1

Files:
- web-app/lib/widgets/dashboard/franchise_picker_dropdown.dart

Goal: Switching franchise must NOT navigate to Admin home. Stay on current surface (HQ / shell / Admin).

Task:
Quote first 10–12 lines of the loaded file.
Respond ONLY with FILE / BEFORE / AFTER. Prefer multi-line BEFORE.

Locate onChanged / selection handler that changes franchise then pushes or goesNamed to admin dashboard / AdminDashboardScreen / '/' admin home.

AFTER: update FranchiseProvider (or existing switch API) only — remove Navigator push/go that forces Admin dashboard.
Keep any load/reload side effects that are data-only.

If file is not the switch site, reply No change needed and name the real path in one line of prose after (still prefer No change needed if unsure).
Do not invent routes.
If already data-only → No change needed
