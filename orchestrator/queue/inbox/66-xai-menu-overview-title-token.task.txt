# id: xai-menu-overview-title-token
# agent: web_frontend
# backend: xai
# priority: 5
# max_regions: 1
# context: coding_min

backend: xai
Role: web_frontend
max_regions: 1

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/onboarding_menu_screen.dart

Goal: Onboarding overview headline uses DesignTokens.primaryColor for franchise-name emphasis.

Task:
Quote the exact first 10–12 lines of the loaded file.
Respond ONLY with FILE / BEFORE / AFTER.

Locate:
```dart
            Text(
              'Onboarding – ${franchise?.name ?? franchiseId}',
              style: Theme.of(context).textTheme.headlineMedium,
            ),
```

AFTER:
```dart
            Text(
              'Onboarding – ${franchise?.name ?? franchiseId}',
              style: Theme.of(context).textTheme.headlineMedium?.copyWith(
                    color: DesignTokens.primaryColor,
                    fontWeight: FontWeight.w600,
                  ),
            ),
```

DesignTokens already imported. Do not change step cards or navigation.
If title already uses DesignTokens.primaryColor → No change needed
