# id: 06-collapsible-panel-radius
# agent: web_frontend
# priority: 60

Using web-app/lib/widgets/collapsible_panel.dart and web-app/lib/config/design_tokens.dart:

1. Quote the exact first 10–12 lines of BOTH real files.
2. Propose ONLY one small change: ensure BorderRadius.circular uses DesignTokens.adminCardRadius (already used in places). Remove any remaining hard-coded radius if present; otherwise add a one-line clarifying comment near the shape.
3. Do not invent DesignTokens members or add methods/fields.
4. Show exact ## BEFORE / ## AFTER for that small region only.
5. If either file cannot be quoted → reply only: FAILED TO LOAD
