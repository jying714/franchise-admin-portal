# id: t5-06-role-badge-admin-secondary
# agent: web_frontend
# priority: 45

Using web-app/lib/widgets/dashboard/role_badge.dart and web-app/lib/config/design_tokens.dart:

1. Quote the exact first 10–12 lines of BOTH real files.
2. Propose ONLY one small change: for the 'Admin' case, set badgeColor to DesignTokens.secondaryColor (the Color getter — NEVER currentSecondaryColor, NEVER *ColorHex as Color).
3. BEFORE must be a contiguous region copied EXACTLY from the injected source (case 'Admin': badgeColor = colorScheme.secondary). If you cannot find an exact match → reply only: FAILED TO LOAD.
4. If Admin already uses DesignTokens.secondaryColor → reply ONLY with: No change needed.
5. Do not invent members. Do not rewrite the whole switch. Leave Owner/default alone.
6. Show exact ## BEFORE / ## AFTER for that tiny region only (fences must differ, or No change needed).
