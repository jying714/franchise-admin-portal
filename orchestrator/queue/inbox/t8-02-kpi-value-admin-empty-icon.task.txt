# id: t8-02-kpi-value-admin-empty-icon
# agent: web_frontend
# priority: 71

Using web-app/lib/widgets/financials/kpi_card.dart and web-app/lib/widgets/admin/admin_empty_state_widget.dart:

DUAL-EDIT PILOT — change exactly ONE region in EACH file (two independent micro-edits).

1. Quote the exact first 10–12 lines of BOTH real files.
2. In kpi_card.dart ONLY: set the value Text color to DesignTokens.primaryColor (never currentPrimaryColor, never *ColorHex as Color). If already DesignTokens.primaryColor → that file needs no change.
3. In admin_empty_state_widget.dart ONLY: set the fallback Icon color to DesignTokens.secondaryColor (never currentSecondaryColor, never *ColorHex as Color). If already DesignTokens.secondaryColor → that file needs no change.
4. Do not rewrite whole widgets. Do not change kpi Icon, admin empty button, or any other region.
5. Show labeled pairs:
   ## BEFORE kpi_card.dart / ## AFTER kpi_card.dart
   ## BEFORE admin_empty_state_widget.dart / ## AFTER admin_empty_state_widget.dart
   Or for a file that is already correct, write: No change needed for <path>.
6. BEFORE regions must be contiguous copies from injected source. If either file cannot be quoted → reply only: FAILED TO LOAD.
