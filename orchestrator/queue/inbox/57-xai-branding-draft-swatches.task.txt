# id: xai-branding-draft-swatches
# agent: web_frontend
# backend: xai
# priority: 3
# max_regions: 2

backend: xai
Role: web_frontend
max_regions: 2

Files:
- web-app/lib/admin/hq_owner/screens/design_branding_screen.dart

Goal: Primary/secondary preview swatches use valid draft hex when present.

Task:
Quote the exact first 10–12 lines of the loaded file.
Respond ONLY with FILE/BEFORE/AFTER. **One FILE path once** (two sequential BEFORE/AFTER pairs under the same FILE header is OK).

Region 1 — insert helper after `_isValidHex` method (before `_saveBranding`):
```dart
  Color? _colorFromHex(String raw) {
    final n = _normalizeHex(raw);
    if (!_isValidHex(n)) return null;
    final v = int.tryParse(n.substring(1), radix: 16);
    if (v == null) return null;
    return Color(0xFF000000 | v);
  }
```
BEFORE must be a contiguous on-disk slice spanning end of `_isValidHex` and start of `_saveBranding`.

Region 2 — swatch Row currently uses `color: DesignTokens.primaryColor` / `secondaryColor`.
Change to:
`color: _colorFromHex(draftPrimaryHex) ?? DesignTokens.primaryColor`
and
`color: _colorFromHex(draftSecondaryHex) ?? DesignTokens.secondaryColor`

Do not change the draft label string in this task.
No new DesignTokens fields.
If swatches already use draft hex → No change needed
