# id: 03-active-plan-banner-polish
# agent: web_frontend
# priority: 30

Using web-app/lib/admin/hq_owner/widgets/active_plan_banner.dart and web-app/lib/config/design_tokens.dart:

1. Quote the exact first 10–12 lines of BOTH real files.
2. Propose ONLY one small polish: ensure any primary/secondary color used for accent or text comes from DesignTokens.primaryColor or DesignTokens.secondaryColor (live path). Replace any hard-coded color if present; otherwise add a clarifying comment only.
3. Do not invent DesignTokens members, do not add fields/getters/methods.
4. Show exact ## BEFORE / ## AFTER for the surgical region only.
5. If either file cannot be quoted → reply only: FAILED TO LOAD
