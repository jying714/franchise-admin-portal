Role: web_frontend

File: web-app/lib/admin/hq_owner/owner_hq_dashboard_screen.dart

Task: Quote first 10–12 lines. Locate Onboarding Progress Consumer.

Required: Consumer (or watch) must use OnboardingProgressProviderImpl so notifyListeners rebuilds the card — not only abstract shared.OnboardingProgressProvider via non-listenable ProxyProvider.

If already Consumer<OnboardingProgressProviderImpl> → No change needed.
If still abstract-only Consumer → switch type + add import for the impl. Do not change step keys or Continue navigation.

Rules: quote-first; this file only; no new provider methods.
