# id: xai-overview-step-complete-indicator
# agent: web_frontend
# backend: xai
# priority: 3
# max_regions: 2
# context: coding_min

backend: xai
Role: web_frontend
max_regions: 2

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/onboarding_menu_screen.dart

Goal: Overview step cards show read-only complete/pending from progress (display only — no mark on cards).

Task:
Quote first 10–12 lines of the loaded file.
Respond ONLY with FILE / BEFORE / AFTER.

OnboardingStepCard already has a `completed:` argument. Ensure each of the 4 cards passes:
- progressProvider.isStepComplete('onboarding_feature_setup')
- progressProvider.isStepComplete('onboarding_menu_foundation')
- progressProvider.isStepComplete('onboardingMenuItems')
- progressProvider.isStepComplete('onboardingReview')

If already wired, check OnboardingStepCard visual: when completed==true show a check icon or muted success style.

Prefer editing only onboarding_menu_screen.dart. If the card widget ignores `completed`, one small region in:
web-app/lib/admin/hq_owner/onboarding/widgets/onboarding_step_card.dart
is allowed (max_regions covers it) — show a Icons.check_circle when completed.

Do not add mark-complete buttons on Overview.
If already correct visually → No change needed
