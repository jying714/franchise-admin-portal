# id: t2-04-branded-loading-primary
# agent: web_frontend
# priority: 13

Using web-app/lib/widgets/branded_loading_screen.dart and web-app/lib/config/design_tokens.dart:

1. Quote the exact first 10–12 lines of BOTH real files.
2. Propose ONLY one small change: give the CircularProgressIndicator a color using DesignTokens.primaryColor (the Color getter — NEVER currentPrimaryColorHex or any *Hex string treated as Color).
3. BEFORE must be a contiguous region copied EXACTLY from the injected source (e.g. the const CircularProgressIndicator() line and its surrounding lines as they appear). If you cannot find an exact match → reply only: FAILED TO LOAD.
4. If the indicator already uses DesignTokens.primaryColor → reply ONLY with: No change needed.
5. Do not add fields, getters, methods, or change any other logic.
6. Show exact ## BEFORE / ## AFTER for that tiny region only (fences must differ, or No change needed).
