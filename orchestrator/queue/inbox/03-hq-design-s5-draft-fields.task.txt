# id: hq-design-s5-draft-fields
# agent: web_frontend
# priority: 30

Using web-app/lib/admin/hq_owner/screens/design_branding_screen.dart:

Quote the exact first 12 lines of the real file.

Convert DesignBrandingScreen from StatelessWidget to StatefulWidget so local draft state can drive the preview.

Add four draft fields (TextEditingController or equivalent local state) initialized from live values:
- app name (from DesignTokens.currentAppName)
- logo URL (from DesignTokens.currentLogoUrl or empty)
- primary hex (from FranchiseProvider.currentPrimaryColorHex)
- secondary hex (from FranchiseProvider.currentSecondaryColorHex)

Wire the Live Preview section so it reflects the draft values (name, logo URL with fallback, swatches from draft hex parsed only if needed for display — prefer keeping Color swatches from DesignTokens live colors if draft hex parsing is risky; do not invent DesignTokens members).

Do not implement Save persistence or Firestore writes. Do not invent new fields on DesignTokens / BrandingConfig / FranchiseProvider.

Show the minimal surgical ## BEFORE / ## AFTER blocks needed (class header + body regions). Keep changes only inside this file.
