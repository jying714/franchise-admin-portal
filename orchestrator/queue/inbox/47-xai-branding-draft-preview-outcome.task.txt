# id: xai-branding-draft-preview-outcome
# agent: web_frontend
# backend: xai
# priority: 4
# max_regions: 2

backend: xai
Role: web_frontend
max_regions: 2

Files:
- web-app/lib/admin/hq_owner/screens/design_branding_screen.dart

Goal: Live preview swatches follow valid draft hex; draft section label reflects v1.1 Save (no longer "Save not wired yet").

Task:
Quote the exact first 10–12 lines of the loaded file.

Region 1 — fix stale label (exact on-disk):
```dart
                      Text(
                        'Draft (local only — Save not wired yet)',
                        style: Theme.of(context).textTheme.titleSmall?.copyWith(
                              fontWeight: FontWeight.w600,
                              color: DesignTokens.secondaryTextColor,
                            ),
                      ),
```
AFTER: change string only to: 'Draft fields (Save writes franchise + config/ui_config)'

Region 2 — draft-aware swatch colors:
Locate the Row that calls _swatchColumn with color: DesignTokens.primaryColor / secondaryColor.
Add a private helper on the State class if needed:
```dart
Color? _colorFromHex(String raw) {
  final n = _normalizeHex(raw);
  if (!_isValidHex(n)) return null;
  final v = int.tryParse(n.substring(1), radix: 16);
  if (v == null) return null;
  return Color(0xFF000000 | v);
}
```
Pass color: _colorFromHex(draftPrimaryHex) ?? DesignTokens.primaryColor (and same for secondary).

Constraints:
- No new DesignTokens / BrandingConfig fields
- Do not change _saveBranding payload keys
- If label already accurate and swatches already use draft hex → No change needed
