# id: xai-review-step-chrome-4step
# agent: web_frontend
# backend: xai
# priority: 7

backend: xai
Role: web_frontend

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/onboarding_review_screen.dart

Task:
Quote the exact first 10–12 lines of the loaded file.

Product truth: HQ onboarding is a **4-step** product (Feature Setup, Menu Foundation, Menu Items, Review & Publish). The review screen still shows stale 6-step chrome.

Locate this region in `build` (inside the Scaffold body Column):
```dart
OnboardingProgressIndicator(
  currentStep: 6,
  totalSteps: 6,
  stepLabel: loc.onboardingStepLabel(6, 6),
),
```

Required change (only this region):
```dart
OnboardingProgressIndicator(
  currentStep: 4,
  totalSteps: 4,
  stepLabel: loc.onboardingStepLabel(4, 4),
),
```

Do not change validation, providers, publish button, or any other UI.
Do not invent new widgets or progress keys.

APPLY SAFETY:
- Exactly one BEFORE/AFTER region preferred.
- Do not touch imports.

If the indicator already uses currentStep: 4 / totalSteps: 4 → reply only: No change needed

Rules:
1. Quote-first.
2. Edit only this file.
3. Proposal only.
