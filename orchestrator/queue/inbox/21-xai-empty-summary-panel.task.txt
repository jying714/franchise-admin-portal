# id: xai-empty-summary-panel
# agent: web_frontend
# backend: xai
# priority: 5

backend: xai
Role: web_frontend

Files:
- web-app/lib/admin/hq_owner/onboarding/widgets/onboarding_summary_panel.dart

Task:
Quote the exact first 10–12 lines of the loaded file (file may be nearly empty).

This file is currently a stub (~empty). Provide a minimal no-op placeholder widget so imports do not break:
```dart
import 'package:flutter/material.dart';

/// Placeholder HQ onboarding summary panel (v1 uses ReviewSummaryTable instead).
class OnboardingSummaryPanel extends StatelessWidget {
  const OnboardingSummaryPanel({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
```

Do not invent progress logic or Firestore. Prefer one full-file BEFORE/AFTER if the file is empty/stub.
If a real panel widget already exists → reply only: No change needed

Rules:
1. Quote-first (or note empty).
2. Edit only this file.
3. Proposal only.
