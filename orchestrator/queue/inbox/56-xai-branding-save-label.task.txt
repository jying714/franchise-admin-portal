# id: xai-branding-save-label
# agent: web_frontend
# backend: xai
# priority: 3
# max_regions: 1

backend: xai
Role: web_frontend
max_regions: 1

Files:
- web-app/lib/admin/hq_owner/screens/design_branding_screen.dart

Goal: Fix stale draft label that claims Save is not wired (v1.1 Save already exists).

Task:
Quote the exact first 10–12 lines of the loaded file.
Respond ONLY with one FILE / BEFORE / AFTER. Single FILE path once.

Locate exact on-disk:
```dart
                      Text(
                        'Draft (local only — Save not wired yet)',
                        style: Theme.of(context).textTheme.titleSmall?.copyWith(
                              fontWeight: FontWeight.w600,
                              color: DesignTokens.secondaryTextColor,
                            ),
                      ),
```

AFTER: change the string only to:
'Draft fields (Save writes franchise + config/ui_config)'

Do not add helpers or change swatches.
If label already accurate → No change needed
