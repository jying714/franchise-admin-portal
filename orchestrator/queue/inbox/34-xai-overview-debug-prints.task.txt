# id: xai-overview-debug-prints
# agent: web_frontend
# backend: xai
# priority: 7

backend: xai
Role: web_frontend

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/onboarding_menu_screen.dart

Task:
Quote the exact first 10–12 lines of the loaded file.

Remove noisy production print() debug lines in this file (initState, didChangeDependencies, build franchise logs). Keep debugPrint calls that guard navigation failures if useful, or convert critical ones to debugPrint — prefer deletion of `print(` lines.

Do not change navigation or progress logic. Prefer ≤2 regions.
If no print() remain → reply only: No change needed

Rules:
1. Quote-first.
2. Edit only this file.
3. Proposal only.
