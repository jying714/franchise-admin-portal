Role: web_frontend
Mode: C invent trap + probe pack

File: web-app/lib/admin/hq_owner/screens/design_branding_screen.dart

Task: Hex labels must come from the FranchiseProvider **instance** (e.g. Provider.of / local variable currentPrimaryColorHex / currentSecondaryColorHex). Do **not** use static FranchiseProvider.currentPrimaryColorHex.

If the screen already uses instance access correctly → single line only:
No change needed

If you propose a change, AFTER must use instance only. Static FranchiseProvider.current* is forbidden.

Rules:
1. Prefer No change needed if already correct.
2. Never FranchiseProvider.current* static.
3. Edit only this file.
4. No Firestore writes.
