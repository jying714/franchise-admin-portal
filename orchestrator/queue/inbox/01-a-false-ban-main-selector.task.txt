Role: web_frontend
Mode: A false-ban probe + probe pack

File: web-app/lib/main.dart

Task: Verify FranchiseAuthenticatedRoot already uses Selector on AdminUserProvider keyed by user id|roles (shell rebuild only on identity/role change). The file may contain existing FirebaseFirestore.instance.collection('franchises') in bootstrap — that is live code, not something to invent or rewrite.

If Selector is already present → reply with a single line only:
No change needed

Do NOT emit full-class BEFORE/AFTER. Do NOT rewrite branding fetch.

Rules:
1. Prefer single-line No change needed.
2. Edit only this file.
3. No new Firestore paths.
4. No FranchiseProvider() zero-arg.
