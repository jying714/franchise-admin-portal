# id: xai-menu-items-incomplete-toggle
# agent: web_frontend
# backend: xai
# priority: 6

backend: xai
Role: web_frontend

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/onboarding_menu_items_screen.dart

Task:
Quote the exact first 10–12 lines of the loaded file.

Product bug: Mark complete updates the HQ progress card, but clicking again to unmark/incomplete does not.

Required:
1. Find the control that marks the menu-items onboarding step complete.
2. Ensure the product progress key is **exactly** `onboardingMenuItems` (not `menu_items`).
3. Wire incomplete / toggle-off to call `markStepIncomplete('onboardingMenuItems')` (or equivalent existing API on the progress provider already used in this file).
4. Complete path must use `markStepComplete('onboardingMenuItems')` if not already.
5. Do not invent new provider methods or Firestore paths.
6. Do not change navigation, shell routing, or other steps.

APPLY SAFETY:
- Max **2** BEFORE/AFTER regions.
- Do not remove imports still referenced.
- Method deletes: empty AFTER body, not a lone `}`.

If toggle already correctly marks incomplete with key `onboardingMenuItems` → reply only: No change needed

Rules:
1. Quote-first from real source.
2. Edit only this file.
3. Proposal only — human /approve confirm.
4. No Admin onboarding paths; no top-level onboarding_progress/{id}.
