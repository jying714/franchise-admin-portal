# id: xai-menu-save-delete-outcome
# agent: web_frontend
# backend: xai
# priority: 3
# max_regions: 2

backend: xai
Role: web_frontend
max_regions: 2

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/onboarding_menu_items_screen.dart

Goal: Dirty-row Save and list Delete work end-to-end using existing MenuItemProvider APIs (persistChanges + remove/delete + snackbars).

Task:
Quote the exact first 10–12 lines of the loaded file.

Region 1 — wire dirty Save (exact on-disk):
```dart
                              ElevatedButton(
                                  onPressed: () async {
                                    /* your exact persistChanges code */
                                  },
                                  child: Text(loc.saveChanges)),
```
AFTER: onPressed must await Provider.of<shared.MenuItemProvider>(context, listen: false).persistChanges() inside try/catch with success/failure SnackBars; use mounted checks.

Region 2 — wire Delete (exact on-disk):
```dart
                                      onDelete: () async {
                                        /* your exact delete dialog unchanged */
                                      },
```
AFTER: show confirm AlertDialog; on confirm call the closest existing MenuItemProvider remove/delete API already used in this codebase (e.g. removeMenuItem / deleteMenuItem if present on the provider), then await persistChanges(); snackbars on success/fail. Do not invent service methods.

Constraints:
- Edit only this file; ≤2 regions
- No new DesignTokens / BrandingConfig / FeatureConfig fields
- Keep shared import alias as-is
- If both regions already real (not stubs) → reply only: No change needed
