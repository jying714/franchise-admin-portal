# id: xai-menu-premium-banner-color
# agent: web_frontend
# backend: xai
# priority: 8

backend: xai
Role: web_frontend

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/onboarding_menu_items_screen.dart

Task:
Quote the exact first 10–12 lines of the loaded file.

Locate the FeatureGateBanner child hard-coded yellow box:
```dart
child: Container(
  width: double.infinity,
  height: 60,
  color: Colors.yellow.shade50,
  alignment: Alignment.center,
  child: const Text(
      'Menu Item Customization is a premium feature.'),
),
```

Replace Colors.yellow.shade50 with a theme surface variant already available:
```dart
color: colorScheme.surfaceContainerHighest,
```
or if that token is unavailable in this Flutter version use `colorScheme.surfaceVariant`.

Prefer one region. Do not invent DesignTokens members.
If already theme-colored → reply only: No change needed

Rules:
1. Quote-first.
2. Edit only this file.
3. Proposal only.
