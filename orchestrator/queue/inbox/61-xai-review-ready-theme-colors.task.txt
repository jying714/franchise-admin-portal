# id: xai-review-ready-theme-colors
# agent: web_frontend
# backend: xai
# priority: 7
# max_regions: 1

backend: xai
Role: web_frontend
max_regions: 1

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/onboarding_review_screen.dart

Goal: Publish-ready check icon/text use theme-aware colors instead of hard-coded Colors.green[700].

Task:
Quote the exact first 10–12 lines of the loaded file.
Respond ONLY with FILE / BEFORE / AFTER. One region. Prefer multi-line BEFORE.

Locate the ready-to-publish branch that uses `Colors.green[700]` for Icon and Text.
AFTER: use `colorScheme.primary` (or `theme.colorScheme.primary`) for both Icon and Text color — `colorScheme` is already available in `_OnboardingReviewContent.build`.

Do not change publish button logic or progress keys.
If already theme-aware → No change needed
