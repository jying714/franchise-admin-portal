# id: xai-branding-class-doc
# agent: web_frontend
# backend: xai
# priority: 8

backend: xai
Role: web_frontend

Files:
- web-app/lib/admin/hq_owner/screens/design_branding_screen.dart

Task:
Quote the exact first 10–12 lines of the loaded file.

Update the class-level dartdoc only. Current text claims "No Firestore writes in v1" which is stale after v1.1.

BEFORE region is the dartdoc block above `class DesignBrandingScreen`.
AFTER should state: HQ Design & Branding editor; draft fields + live preview; Save merges primaryColorHex/secondaryColorHex/appName/logoUrl to franchises/{id} and config/ui_config then setBrandingFromFranchiseDoc.

Do not change class body. Prefer one region.
If dartdoc already describes v1.1 save → reply only: No change needed

Rules:
1. Quote-first.
2. Edit only this file.
3. Proposal only.
