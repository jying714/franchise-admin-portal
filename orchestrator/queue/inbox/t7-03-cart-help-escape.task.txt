# id: t7-03-cart-help-escape
# agent: web_frontend
# priority: 62

Using web-app/lib/widgets/header/cart_icon_badge.dart and web-app/lib/widgets/header/help_icon_button.dart:

1. Quote the exact first 10–12 lines of BOTH real files.
2. You may change ONLY ONE region in cart_icon_badge.dart. Do NOT edit help_icon_button.dart at all.
3. Check whether the cart Icon color already uses a real DesignTokens Color getter (e.g. DesignTokens.foregroundColor).
4. If it already uses any real DesignTokens Color getter → reply ONLY with: No change needed.
5. Do not swap to primaryColor if already correct. Do not invent members. Do not touch the sibling file.
6. Show exact ## BEFORE / ## AFTER only if a real change is required; otherwise reply ONLY: No change needed.
