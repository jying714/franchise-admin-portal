# id: xai-menu-persist-dirty
# agent: web_frontend
# backend: xai
# priority: 5

backend: xai
Role: web_frontend

Files:
- web-app/lib/admin/hq_owner/onboarding/screens/onboarding_menu_items_screen.dart

Task:
Quote the exact first 10–12 lines of the loaded file.

Locate the dirty-row Save button stub:
```dart
ElevatedButton(
    onPressed: () async {
      /* your exact persistChanges code */
    },
    child: Text(loc.saveChanges)),
```

Replace the onPressed body with a real call to the existing MenuItemProvider API already used in this file:
```dart
onPressed: () async {
  final menuProvider =
      Provider.of<shared.MenuItemProvider>(context, listen: false);
  try {
    await menuProvider.persistChanges();
    if (mounted) {
      ScaffoldMessenger.of(context).showSnackBar(
        const SnackBar(content: Text('Changes saved')),
      );
    }
  } catch (e) {
    if (mounted) {
      ScaffoldMessenger.of(context).showSnackBar(
        SnackBar(content: Text('Save failed: $e')),
      );
    }
  }
},
```

Do not invent service methods. Prefer one BEFORE/AFTER region.
If Save already calls persistChanges() for real → reply only: No change needed

Rules:
1. Quote-first.
2. Edit only this file.
3. Proposal only.
